import { ActivityIndicator } from '@react-native-material/core'
import {View} from 'react-native'
const ListFooter = () => (
    <View>
        <ActivityIndicator size="small" color='#1FC159' />
    </View>
)
export default ListFooter